<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- Structured-style pattern: timestamp level logger message (parseable for log aggregation) -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd'T'HH:mm:ss.SSSZ} %-5level [%logger{36}] %msg%n</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>

    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
    </root>

    <logger name="com.danielagapov.spawn.gateway" level="INFO"/>
    <logger name="org.springframework.cloud.gateway" level="INFO"/>
</configuration>
