# Test configuration for JWT tests
# This is a test-only signing secret - NOT used in production
jwt.signing-secret=dGVzdC1zaWduaW5nLXNlY3JldC1mb3Itand0LXRlc3RpbmctdGhhdC1pcy1sb25nLWVub3VnaC1mb3ItaHMyNTY=

# Database configuration for tests
spring.datasource.url=jdbc:h2:mem:testdb;MODE=MySQL;DB_CLOSE_DELAY=-1
spring.datasource.username=sa
spring.datasource.password=
spring.datasource.driver-class-name=org.h2.Driver
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.open-in-view=false

# Disable flyway for tests (H2 uses ddl-auto=create-drop)
spring.flyway.enabled=false

# OAuth test configuration - dummy values for testing
google.client.id=test-google-client-id
apple.client.id=test-apple-client-id

# Email test configuration - dummy values for testing
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=test@test.com
spring.mail.password=test-password

# Push notification test configuration - dummy values
apns.certificate.path=classpath:test-cert.p8
apns.certificate.password=test-password
apns.production=false
apns.bundle.id=com.test.app

# Redis test configuration - use embedded or mock
spring.cache.type=simple
spring.data.redis.host=localhost
spring.data.redis.port=6379
spring.data.redis.password=
